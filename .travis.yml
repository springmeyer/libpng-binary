language: cpp

compiler:
 - gcc
 - clang

env:
  matrix:
    - TARGET=png PLATFORM=Linux PUBLISH=true QUIET=true

before_install:
 - true

install:
 - true

before_script:
 - true

script:
 - source ./build.sh
 - build $TARGET